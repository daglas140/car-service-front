import { UpdateControlDirective } from './update-control.directive';

describe('UpdateControlDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateControlDirective();
    expect(directive).toBeTruthy();
  });
});
